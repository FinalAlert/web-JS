Мій To-Do List

Контрольні запитання

1. Як працює метод querySelector() порівняно з getElementById()?
getElementById() - шукає тільки по id (швидше)
querySelector() - приймає будь-який CSS-селектор (#id, .class тощо)

2. Що таке делегування подій і які його переваги?
Обробник вішається на батьківський елемент
Переваги: працює з динамічними елементами, менше навантаження на пам'ять

3. Як фази подій впливають на обробку подій?
Захоплення (вниз від document) → Ціль → Спливання (вгору)
Впливає на порядок виконання обробників

4. Які типові помилки виникають при роботі з DOM і як їх уникнути?
Неправильні селектори - перевіряти існування елементів
XSS-атаки - використовувати textContent замість innerHTML
Втрата посилань - зберігати змінні з елементами

5. Чому важливо використовувати trim() при роботі з введеними даними?
Видаляє пробіли на початку/в кінці
Уникає "невидимих" завдань з пробілів
Запобігає випадковим порожнім записам
